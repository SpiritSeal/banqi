build/
*.o
*.d
a.out
tests/run_tests
emsdk/
.DS_Store
node_modules/

# Claude Code agent worktrees (per-subagent isolated checkouts).
.claude/

# Compiled WebAssembly artifacts — built locally via `make wasm` and in CI
# via the emsdk install step. Not source.
web/banqi.js
web/banqi.wasm

# Relay runtime artifacts.
server/.env
server/banqi.db
server/banqi.db-wal
server/banqi.db-shm
server/test-banqi-*.db*
