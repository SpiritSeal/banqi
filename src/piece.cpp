// All piece helpers are inline in piece.hpp; this TU exists to give the
// header a buildable .o so the Makefile's per-source object rules are
// uniform.
#include "piece.hpp"
namespace banqi {}  // (intentionally empty)
